version: 1
backend:
  phases:
    preBuild:
      commands:
        - echo "Skipping backend build for now"
    build:
      commands:
        - echo "Skipping backend build for now"
frontend:
  phases:
    preBuild:
      commands:
        - chmod +x ./amplify-build.sh
    build:
      commands:
        - ./amplify-build.sh
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
