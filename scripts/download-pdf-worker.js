// This script is no longer needed as we're using a simpler PDF viewer approach
// that doesn't require a worker file

console.log('PDF worker script is no longer needed - using native PDF viewer');

